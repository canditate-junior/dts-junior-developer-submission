# dts-junior-developer-submission
A full-stack, containerised Task Management System built with NestJS (Backend), React + Vite (Frontend), and PostgreSQL. Implements strict DTO validation and Docker orchestration.
